package edu.ithaca.dragon.bank;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class adminTest {
    @Test
    void freezeUnfreeze() {
        //TODO
        /*  Going to test freezing and unfreezing an account
        To freeze an account requires that we add a boolean "active" attribute to the BankAccount class
        This method will checkbalance(), then freeze the account, checkbalance() and unfreeze, checkbalance() to show
        that the method works. This can be done with more than just the checkblanace() method.
         */

        //I couldn't complete because for some reason I don't have access to the diagram as of current, and didn't want
        //to make a mistake
    }
}
